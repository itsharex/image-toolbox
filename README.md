# Image Toolbox (图片工具箱)

![Version](https://img.shields.io/badge/version-0.1.0-blue) ![Tauri](https://img.shields.io/badge/Tauri-v2-orange) ![Vue](https://img.shields.io/badge/Vue-3-green)

一个基于 **Tauri v2** + **Vue 3** 构建的现代化、高性能桌面端图片处理工具。专为批量处理任务设计，支持 Git 自动化工作流。

## ✨ 核心功能

### 1. 🖼️ 图片批量压缩
- **高性能处理**：底层调用 **FFmpeg** 进行极速图片转码与缩放。
- **灵活的缩放模式**：
  - **固定尺寸 (Fixed)**：指定宽/高，支持“裁剪 (Crop)”或“留白填充 (Pad)”模式。
  - **按比例 (Ratio)**：支持 0.1x - 2.0x 任意倍率缩放。
- **多格式支持**：支持导出为 PNG, JPG, WebP 格式。
- **现代化 UI**：全暗色玻璃拟态 (Glassmorphism) 风格界面，操作流畅。

### 2. 🤖 自动化工作流 (Git Integration)
- **智能检测**：自动检测输入/输出目录是否位于 Git 仓库内。
- **自动提交**：支持在处理完成后自动执行 `git add .` -> `git commit` -> `git push`。
- **精细控制**：批量处理模式下，可为每个任务单独配置是否启用 Git 自动提交。

### 3. 📂 强大的配置管理 (Profile System)
- **JSON 配置驱动**：支持加载外部 `settings.json` 文件（支持多 Profile）。
- **批量任务队列**：一键加载多个配置项，通过“确认模式 (Review Mode)”预览所有任务详情（路径、参数），一键批量执行。
- **路径自动解析**：智能解析配置文件中的相对路径。

### 4. ⚙️ 环境管理 (Settings)
- **FFmpeg 一键安装**：
  - 内置检测机制，自动识别系统 PATH 中的 FFmpeg。
  - 提供**一键下载安装**向导：支持官方源 (Gyan.dev) 和 Cloudflare 镜像源。
  - 自动配置环境变量 (User PATH)，无需繁琐的手动设置。
- **CLI 集成**：支持将本软件添加至系统 PATH，方便从终端启动。

## 🛠️ 技术栈

*   **Core**: [Tauri v2](https://v2.tauri.app/) (Rust)
*   **Frontend**: Vue 3, TypeScript, Vite, Tailwind CSS v4
*   **Icons**: Lucide Vue Next
*   **Shell Integration**: PowerShell, Git, FFmpeg

## 🚀 快速开始

### 开发环境

1.  **安装依赖**
    ```bash
    npm install
    ```

2.  **启动开发服务器**
    ```bash
    npm run tauri dev
    ```

### 构建发布

```bash
npm run tauri build
```

## 📦 项目结构

```text
├── src/
│   ├── components/
│   │   ├── ImageCompressor.vue  # 图片压缩核心逻辑与UI
│   │   └── Settings.vue         # 设置、环境检测与安装向导
│   ├── App.vue                  # 主布局与导航
│   └── assets/                  # 静态资源
├── src-tauri/
│   ├── capabilities/            # 权限配置 (Git, Shell, FS)
│   ├── src/                     # Rust 后端代码
│   └── tauri.conf.json          # Tauri 配置文件
└── ...
```

## 📝 配置文件示例 (`settings.json`)

```json
{
  "profile_name": {
    "enable": true,
    "description": "任务描述...",
    "input_folder": "./input",
    "output_folder": "./output",
    "format": "webp",
    "resize_method": "fixed",
    "width": 1920,
    "height": 1080,
    "fixed_mode": "crop"
  }
}
```

## 📄 License

MIT